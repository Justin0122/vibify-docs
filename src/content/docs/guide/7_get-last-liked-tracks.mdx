---
title: Get Last Liked Tracks
description: This example shows how to get the user's last liked tracks.
---

import Response from '../../../pages/responses/lastLiked.mdx';

To display the user's last liked tracks, you can use the `getLastLikedTracks` method. This method returns an array of the user's last liked tracks.

```js
async function getLastLikedTracks(userId, amount){
    try {
        const tracks = await vibify.getLastLikedTracks(userId, amount);
        console.log('Last Liked Tracks:', tracks);
    } catch (error) {
        console.error('Error:', error);
    }
}

getLastLikedTracks('user-id', 1);
```

### Returns

<div data-pagefind-ignore>
    <Response data-pagefind-ignore />
</div>